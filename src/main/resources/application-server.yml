server:
  path: https://smart-digital-dashboard.ntubimdbirc.tw/backend
  image:
    name: image
    path: 'C:\sodd-backend\UploadFile\${server.image.name}\'
  file:
    name: file
    path: 'C:\sodd-backend\UploadFile\${server.file.name}\'
logging:
  level:
    sqltiming: DEBUG
    resultsettable: DEBUG
